const cardImg = (props) => {
  return (
    <>
      <div className="tabImag">
        <img className="genericBox" src={props.imgsrc} />
      </div>
    </>
  );
};

export default cardImg;
